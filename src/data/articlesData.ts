import { ArticleReport } from '../types';

/**
 * Expedition reports, river guides and articles database.
 * Empty by default for clean initial launch.
 * Managed and populated by the Administrator via the Admin Panel.
 */
export const ARTICLES_DATA: ArticleReport[] = [];
