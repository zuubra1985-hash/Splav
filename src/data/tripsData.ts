import { CompanionTrip } from '../types';

/**
 * Companion expeditions and trips database.
 * Empty by default for clean initial launch.
 * Managed and populated by tourists, organizers, and the Administrator.
 */
export const COMPANION_TRIPS_DATA: CompanionTrip[] = [];
